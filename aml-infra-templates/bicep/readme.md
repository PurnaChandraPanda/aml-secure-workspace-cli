Bicep templates for reference

- install bicep from powershell or git bash
```
winget install -e --id Microsoft.Bicep
```

- validate the bicep version installed (if not working, restart terminal or app)
```
bicep --version
```

