
## How to run?
- Rename `code/example.tfvars` to `code/terraform.tfvars`
- Update the exposed variable values correctly in `code/terraform.tfvars`

- Run the template

```
cd code
az login
terraform init
terraform apply
```

## reference
[Microsoft.MachineLearningServices : terraform](https://learn.microsoft.com/en-us/azure/templates/microsoft.machinelearningservices/workspaces?pivots=deployment-language-terraform)

